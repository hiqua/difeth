--- 
+++ 
@@ -7,7 +7,7 @@
     bytes memory b_hash = bytes(sha256);
     
     if ( b_hash.length == 64 ){
-      if ( proofs[sha256] == 0 ){
+      if ( proofs[sha256] != 0 ){
         proofs[sha256] = block.timestamp;
       }
     }
